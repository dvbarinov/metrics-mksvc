groups:
  - name: test-alerts
    rules:
      - alert: TestAlert
        expr: vector(1)
        labels:
          severity: warning
          service: test
        annotations:
          summary: "Тестовый алерт"
          description: "Это тестовый алерт для проверки конфигурации"
